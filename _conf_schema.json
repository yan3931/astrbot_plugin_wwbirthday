{
  "isphoto": {
    "description": "是否发送角色图片",
    "type": "bool",
    "hint": "不开启则仅发送纯文本生日提醒",
    "default": "true"
  },
  "list": {
    "description": "群聊白名单",
    "type": "string",
    "hint": "以逗号分隔的群号列表，如：123456789,987654321 仅在此处填写的群号才会启用功能，留空则默认所有群都不启用，",
    "default": ""
  },
  "time": {
    "description": "每日提醒时间",
    "type": "string",
    "hint": "24小时制，格式为 HH:MM ，例如：8:00，13:00 等，注意英文冒号！",
    "default": "09:00"
  },
  "image_download": {
    "description": "是否下载角色图片",
    "type": "bool",
    "hint": "开启后会在更新数据时自动下载角色图片（需要网络）",
    "default": "false"
  },
  "image_timeout": {
    "description": "图片下载超时时间(秒)",
    "type": "int",
    "hint": "下载图片时的超时时间",
    "default": 10
  }
}